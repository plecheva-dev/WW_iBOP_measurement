[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "package1"
version = "0.1.0"
description = "A simple practice package for my functions"
readme = "README.md"
requires-python = ">=3.7"
dependencies = [] # Add external libraries like 'requests' here